<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" android:layout_height="fill_parent" android:scrollbars="none" android:background="#ffffff">

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff" >
    
    <ProgressBar
        android:id="@+id/progressBar1"
        android:layout_marginTop="200dip"
        style="?android:attr/progressBarStyleLarge"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    
    <TextView android:id="@+id/total_income"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#000000"
        android:text="你目前总收入:%s分"
        android:layout_alignParentTop="true"
    />

<TableLayout 
    android:id="@+id/main_table"
    android:stretchColumns="0,1"
    android:background="#ffffff"
    android:layout_below="@id/total_income"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
</TableLayout>

</RelativeLayout>
</ScrollView>